<div class="album text-muted">
  <div class="container">
    <div class="row <%= "sortable" if logged_in? (:sita_admin) %>">
      <div class ="col-md-12">
        <%= link_to "New", new_portfolio_path if logged_in?(:site_admin)  %>
      </div>
      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
    </div>
  </div>
</div>
